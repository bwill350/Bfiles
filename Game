extends Node

signal points_changed(points)

var points: int = 0

func add_points(amount: int):
	points += amount
	points_changed.emit(points)

# Match data
var p1_character := ""
var p2_character := ""

var vs_cpu := true
