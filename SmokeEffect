extends Node2D

@onready var sprite: AnimatedSprite2D = $Sprite

func _ready():
	print("SMOKE SPAWNED")

	global_position += Vector2(0, -50)
	z_index = 100

	call_deferred("_play_smoke")

func _play_smoke():
	sprite.play("smoke")
	await sprite.animation_finished
	queue_free()
